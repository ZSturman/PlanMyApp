import React from 'react';

type TechStackProps = {
    content: {
    };
 };

const TechStack = ({ content }: TechStackProps) => {
  return (
    <div>
      TechStack
    </div>
)};

export default TechStack;