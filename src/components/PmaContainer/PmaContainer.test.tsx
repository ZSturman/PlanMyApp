import React from "react";
import { render } from "@testing-library/react";

import PmaContainer from "./PmaContainer";

describe("PmaContainer", () => {
  test("renders PmaContainer component", () => {
    render(<PmaContainer />);
  });
});
