import React from 'react';

type MonitizationProps = {
    content: {
    };
 };

const Monitization = ({ content }: MonitizationProps) => {
  return (
    <div>
      Monitization
    </div>
)};

export default Monitization;