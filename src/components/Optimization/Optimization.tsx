import React from 'react';

type OptimizationProps = {
    content: {
    };
 };

const Optimization = ({ content }: OptimizationProps) => {
  return (
    <div>
      Optimization
    </div>
)};

export default Optimization;