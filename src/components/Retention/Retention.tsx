import React from 'react';

type RetentionProps = {
    content: {
    };
 };

const Retention = ({ content }: RetentionProps) => {
  return (
    <div>
      Retention
    </div>
)};

export default Retention;