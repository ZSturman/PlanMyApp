import React from "react";

type AnalyticsProps = {
  content: {
    
  };
};

const Analytics = ({ content }: AnalyticsProps) => {
  return (
    <div >
    Analytics
    </div>
  );
};

export default Analytics;
