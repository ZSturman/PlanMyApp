import React from "react";

type DatabaseProps = {
  content: {
    
  };
};

const Database = ({ content }: DatabaseProps) => {
  return (
    <div >
    Database
    </div>
  );
};

export default Database;
