import React from "react";

type DeploymentProps = {
  content: {
    
  };
};

const Deployment = ({ content }: DeploymentProps) => {
  return (
    <div >
    Deployment
    </div>
  );
};

export default Deployment;
